# Production must never fall back to the local in-memory H2 database.
spring.datasource.url=${TRAVELDB_DATABASE_URL}
spring.datasource.username=${TRAVELDB_DATABASE_USERNAME}
spring.datasource.password=${TRAVELDB_DATABASE_PASSWORD}
